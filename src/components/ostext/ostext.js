import createCustomElement from "../../utils/helpers/helpers";

const operationSystemMessage = `The project is written in the system macOS Monterey`;
const systemText = createCustomElement("h3", "ostext", operationSystemMessage);
export default systemText;
